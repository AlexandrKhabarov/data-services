# Local development overrides: hot-reload, exposed DB port
services:
  db:
    ports:
      - "5432:5432"

  backend:
    volumes:
      - ./backend:/app
    command: >
      sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
